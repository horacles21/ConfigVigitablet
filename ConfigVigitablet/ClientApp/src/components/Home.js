import React from 'react';
import { connect } from 'react-redux';

const Home = props => (
  <div>
        <h1>Vigitablet Sigris</h1>
        <h1>Sistema Integral de Gesti&oacute;n de Recursos e Informaci&oacute;n para la Seguridad</h1>
  </div>
);

export default connect()(Home);
